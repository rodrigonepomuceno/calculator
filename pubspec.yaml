name: calculadora
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2
  
flutter_icons:
  android: 'launcher_icon'
  ios: true
  image_path_android: 'assets/calc.png'
  image_path_ios: 'assets/calc.png'

flutter:

  uses-material-design: true
  assets:
    - ./assets/